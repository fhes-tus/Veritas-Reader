package android.app

import android.content.Context

class TimePickerDialog(
    context: Context,
    listener: (Any?, Int, Int) -> Unit,
    hourOfDay: Int,
    minute: Int,
    is24HourView: Boolean
) {
    fun show() {}
}
